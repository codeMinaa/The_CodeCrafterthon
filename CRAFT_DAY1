package main
import "fmt"

func add(a, b int) int {
	
	return a + b
}

func mul(a, b int)int{
	return a * b
}


func main() {
	
	var choice string

	fmt.Println("choose an operation: 1. add 2. mul")
	fmt.Scanln(&choice)
	var a int 
	var b int 

	fmt.Print("Enter a number: ")
	fmt.Scanln(&a)
	fmt.Print("Enter a  second number: ")
	fmt.Scanln(&b)


	if choice == "add" {
		fmt.Print(add(a, b))
	}else if choice == ""
}
